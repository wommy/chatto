---
title: Chatto 0.5.0 (unreleased)
description: Unreleased release notes for Chatto 0.5.0.
---

import ReleaseFeatureCard from "../../../components/release-notes/ReleaseFeatureCard.astro";
import ReleaseFeatureGrid from "../../../components/release-notes/ReleaseFeatureGrid.astro";
import ReleaseHero from "../../../components/release-notes/ReleaseHero.astro";

<ReleaseHero
  version="0.5.0"
  status="Unreleased"
  summary="0.5.0 adds server-wide message search, native social post previews, richer room and admin tools, upload progress, localized timestamps, and many more interface languages."
/>

<ReleaseFeatureGrid>
  <ReleaseFeatureCard title="Search across your server" size="large">
    Find messages across every room you can access, with standalone filters for rooms and authors.
    Language-aware matching handles common word forms, CJK text, exact phrases, and
    conservative typos. Press Cmd-K (or Ctrl-K) and prefix a query with `?` to merge the
    best-ranked matches from all your registered servers into one quick result list.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Rooms are easier to discover and manage" size="large">
    Preview joinable rooms before entering, use natural room names with spaces,
    punctuation, emoji, and any writing system, and manage explicit channel members from
    room settings. Context menus put join, leave, read, and management actions directly
    beside rooms, groups, and servers.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Native social post previews">
    Bluesky and Mastodon links render as native cards with quoted posts, attached media,
    link cards, and concealed content warnings instead of relying on third-party embeds.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="More expressive messages">
    Insert localized timestamps, render GFM tables, and copy message text from its action
    menu. Attachment sends now show per-file progress and preserve failed uploads for retry.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="More useful user profiles">
    Open a user's profile from their user card, add a Markdown bio to your own profile, and see
    the local time that a user chooses to share. Stored time zones are private by default. You
    can use your time zone for date formatting without publishing it on your profile. Bios can
    contain up to 1,000 characters. Local times use your preferred 12-hour or 24-hour format.
    Bots use the same self-profile API when authenticated with their API key.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Notifications that stay useful after you read them">
    Notifications now stay in one chronological list after you read them. Delete an item whenever
    you no longer want to retain it; per-signal-class controls distinguish mentions,
    replies, followed conversations, direct messages, and reactions.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Pinned messages">
    Room managers can pin useful channel messages in a shared sidebar. A subtle dot shows
    when a newer pin has not been viewed.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="More languages and regional choices">
    Choose from complete regional catalogues for English, German, Dutch, Swedish, French,
    Spanish, Portuguese, Norwegian, Polish, Ukrainian, Japanese, Esperanto, Simplified
    Chinese, and Traditional Chinese.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Remote sign-in stays in context" size="small">
    Authentication for another server opens in a focused popup, keeping your main client
    mounted while making the remote server's identity and trust boundary clearer.
  </ReleaseFeatureCard>
</ReleaseFeatureGrid>

## Running and Integrating Chatto

<ReleaseFeatureGrid>
  <ReleaseFeatureCard title="Resource-scoped room administration" size="large">
    Room and room-group settings now live beside the resources they control. Delegated
    managers can configure rooms without entering a monolithic server-admin area, while
    role assignment is bounded by the manager's own authority.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Delete accounts from member administration" size="small">
    Admins with `user.delete-any` can permanently delete another human member from the
    member detail page. They confirm the exact login before deletion; this action does not
    request the admin's password.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Version-aware compatibility">
    Public server discovery reports the running server version. The multi-server client owns
    its minimum expected server release and uses explicit release boundaries to show useful
    upgrade guidance. If a discovered server is incompatible or does not report a valid version,
    the Server Directory opens that server's own client instead of adding it to the current app.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Advertise friendly Chatto servers">
    Server administrators can maintain a public Neighbor directory. The Server
    Directory shows direct recommendations from a user's registered servers. It
    follows a remote server recursively only when both servers advertise each
    other, for at most two mutual hops. Results appear as requests finish, and
    one additional batch can load when a user scrolls near the end. Fixed batch,
    concurrency, timeout, and page-session limits bound browser work.
    Each result identifies the registered servers that supplied the
    recommendation. Server profile cards use a tapestry layout. Mutuality is a
    current client observation, not a verified relationship or trust statement.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Remote clients connect without allowlists" size="large">
    Browser frontends publish a verifiable CIMD identity and exact callback, while Chatto
    Desktop uses its built-in identity. Servers accept bearer-authenticated cross-origin API
    and realtime traffic without CORS or OAuth redirect-origin configuration. Server managers
    can review successfully authorized clients, label them trusted, or block their access.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Bearer sessions renew without another login">
    Human clients use short-lived access tokens and rotating refresh credentials. The bundled
    frontend renews the session window in the background and reconnects realtime in place
    without reopening OAuth, showing a manual warning, or losing the current route. The bundled
    same-origin client uses only its safer HttpOnly cookie and renews it automatically.
    Separately hosted browser clients use Chatto OAuth for renewable bearer sessions.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Faster, leaner restarts">
    Encrypted projection snapshots and faster cold replay shorten restart catch-up on
    servers with substantial history, while more compact timeline state reduces memory use.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="A more practical permission baseline">
    The `everyone` role now acts as a scoped default that named roles and direct-user
    decisions can override at the same or a narrower scope. Fresh announcement rooms also
    give admins an explicit room-level posting grant. The new `message.read` and
    `message.read-interactions` permissions separate broad and relationship-scoped
    channel-room message access from membership. Broad read access includes the narrower
    interaction permission.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Bots activate through normal notifications" size="large">
    Bot integrations can use the same ConnectRPC API and realtime subscription as human
    clients. Direct messages, direct mentions, replies, and followed-thread activity arrive
    as exact notification occurrences. A direct mention in a root or reply follows that
    thread for later activity when notification policy permits it. Each bot can also have up
    to 20 named API keys. Managers can replace one integration key without stopping the others.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Visible cleanup health" size="small">
    Owner diagnostics show whether physical asset cleanup is healthy, catching up, or
    accumulating retries without exposing asset details.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Web Push without setup" size="small">
    Web Push is on by default. The server makes its own VAPID key pair at first start and
    uses the public server URL as its contact address, so operators no longer generate keys
    to give members push notifications. Configured keys still take precedence, and
    `push.enabled = false` turns the feature off.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Test Web Push delivery" size="small">
    Members can send themselves a real test notification from notification settings and get
    direct feedback when a subscription has expired or delivery fails.
  </ReleaseFeatureCard>

  <ReleaseFeatureCard title="Opt-in community badges" size="small">
    Operators can publish aggregate online and registered-member counts through
    Shields.io-compatible endpoints for READMEs and community websites.
  </ReleaseFeatureCard>
</ReleaseFeatureGrid>

## Upgrade Notes

- Existing experimental MCP connections must authorize again. The tester tool
  catalog now needs the room and message read/write MCP scopes. A token that
  has only `chatto:rooms:read` cannot use the expanded catalog.
- Custom clients must remove the testimonial fields from
  `chatto.admin.v1` and use `ListNeighborsResponse.origins` from
  `chatto.discovery.v1`. Upgrade every replica together. Historical testimonial
  text remains in `EVT` and backups, and a rollback to a testimonial-capable
  binary can expose it again.
- Upgrade every Chatto replica before you rely on private time zones. An older replica does not
  understand the sharing setting and can expose a stored time zone. A rollback to an older
  replica has the same risk.

- Remove `current_password` from `chatto.admin.v1.DeleteUserRequest` in custom
  clients before you update generated code. A 0.5 server ignores protobuf
  binary tag `2`, but `current_password` is not part of the 0.5 ProtoJSON
  schema. A 0.5 client does not send the field. An older server can return
  `failed_precondition` when the current session is not fresh.
- The bundled frontend automatically moves a valid 0.4 typed browser session to
  the SCS-backed `chatto_auth_*` path. The migration keeps the existing opaque
  server-side session and does not ask the user to sign in again. An expired,
  revoked, or older `cookie_session.*` record cannot migrate. Affected users must
  sign in once. After migration, active browser sessions renew automatically
  without user action. Chatto 0.6 removes this temporary migration bridge.
- Pre-renewal bearer records are not accepted by 0.5 servers. Affected users sign in once to
  receive the new access/refresh pair. Upgrade every serving replica and bundled client
  together: older binaries treat new access records as sliding credentials and are not safe
  mixed-version peers or rollback targets after issuance. Custom clients must persist and
  serialize `/oauth/token` refresh grants. Generate one cryptographically random
  UUID version 4 `refresh_request_id` for each rotation, and keep that same value
  across a lost-response retry.
- Before or during the upgrade, grant `message.read` to every existing role or
  account that must keep broad channel-room message access. Grant
  `message.read-interactions` where an account or bot must read only related
  threads. A server-level `everyone` grant for `message.read` preserves broad
  access and includes the narrow mode. Chatto never migrates, backfills, or
  reconciles existing RBAC state. Fresh servers receive only the broad grant
  during empty-stream bootstrap. DM membership continues to authorize DM
  reads. Upgrade every replica before you rely on a channel-room deny, missing
  grant, or narrow-read availability.
- Bot integrations must not call `RoomService.StartDM`. Chatto 0.5 denies this
  operation to every bot, including when the DM already exists. A human must
  start the DM before the bot can read or post in it. Upgrade every replica
  before you depend on this boundary because an older replica can still accept
  the call.
- Bot integrations can use `notification_occurrences_replace` on the normal
  realtime subscription as their activation source. Save stable occurrence
  IDs and deduplicate by the referenced message event ID when one message must
  cause only one action. During a mixed-replica rollout, an older replica can
  omit the follow created by a direct root mention. Complete the rollout before
  you depend on later followed-thread activations.
- Existing bot API keys stay valid after upgrade and appear as the default
  named key. Complete the replica rollout before an integration uses a newly
  added named key because older replicas do not recognize it. The experimental
  `RotateBotApiKey` method is removed. Create a named key, move the integration,
  and revoke the old key instead.
- Remove `webserver.oauth_redirect_origins` or its environment-variable
  equivalent. Add an exact `webserver.allowed_origins` entry when a reverse
  proxy serves the bundled frontend from an additional origin. The entry lets
  that origin use cookie authentication and publishes its matching frontend
  OAuth identity. It does not control cross-origin API access or authorize
  foreign clients. `*` permits neither browser cookies nor frontend OAuth
  metadata. The 0.5 OAuth flow requires a CIMD or built-in `client_id`, so
  upgrade clients and servers together. Pre-0.5 clients cannot use the new
  remote-server authorization flow.
- Notifications 2.0 replaces the previous notification service and stored records. Existing
  pending notifications and notification-level preferences are not migrated, so every account
  starts with an empty notification list and the new per-signal-class defaults. Integrations must
  regenerate their clients and move to the new occurrence and policy methods on
  `NotificationService`; the legacy list/get/dismiss RPCs and realtime fields no longer work.
  `NotificationOccurrence.signal` is a typed oneof that combines one cause with its exact
  destination and cause-specific data. Clients must not infer navigation for unsupported
  variants and may show them as generic, dismissible activity. The bundled client uses binary
  protobuf for this forward-compatible fallback. ProtoJSON integrations must ignore unknown
  fields or regenerate before a future signal variant is exposed.
  Upgrade all replicas and bundled clients together.
- Web Push is enabled by default in 0.5. A server that never configured VAPID
  keys now generates a key pair at its next start, keeps it in `RUNTIME_STATE`,
  and shows the push controls to members. No push service is contacted until a
  member grants notification permission on a device. Set `push.enabled = false`
  (`CHATTO_PUSH_ENABLED=false`) to keep the feature hidden. A configured key
  pair keeps working unchanged; `push.vapid_subject` now defaults to
  `webserver.url`, and setting only one of the two VAPID keys is a startup
  error.
- Existing 0.4 Web Push subscription records remain stored, but 0.5 cannot use
  them until the browser registers them again. The bundled client performs this
  refresh without another permission prompt when the user opens it and
  notification permission is still granted. Push delivery can be unavailable
  until that first visit.
- Realtime protocol 2 is required by both the 0.5 client and server. Upgrade every server
  replica and the bundled frontend together; a 0.5 client treats 0.4 servers as incompatible,
  and a 0.5 server no longer accepts protocol 1. Integrations that use `/api/realtime` must
  migrate to the stable protocol 2 reference.
- Upgrade every Chatto server replica from 0.4 to 0.5 together rather than using a rolling
  server deployment. A 0.4 replica can accept a late message edit after another replica has
  deleted that message and can temporarily render its body again. Once every replica runs
  0.5, message edits and deletions share one room lifecycle boundary and deletion tombstones
  remain authoritative during replay.
- Treat the upgrade as forward-only after a 0.5 server accepts traffic. A 0.4
  binary does not understand new notification facts, asset lifecycle facts,
  HLS results, or other 0.5-only events. It can start on the same data but cannot
  provide the complete server state. Deploy a 0.5 fix when possible. For a full
  rollback, restore a tested pre-upgrade backup and accept the loss of later
  writes. Include matching encryption keys in the recovery plan, and back up S3
  assets separately.
- Server discovery no longer publishes `compatibility.protocol_capabilities`, which appeared
  only in unreleased 0.5 development snapshots. The containing `compatibility` message is
  also gone: clients own their minimum supported server release instead of the server
  declaring client requirements. Integrations built against one of those snapshots must
  regenerate from the final 0.5 protobufs and replace capability-list checks with an explicit
  supported-release table. Do not mix clients and servers across this development-only
  schema change.
- Newly processed ordinary videos are HLS-only. Upgrade every replica behind an endpoint
  before allowing new video uploads because older replicas and older clients cannot serve or
  play those results. Existing MP4 renditions remain playable and are not backfilled.
- Review permission configurations that combine an `everyone` deny with named-role or
  direct-user allows. Same-scope or narrower allows can now override that baseline; upgrade
  all replicas together so authorization decisions stay consistent during rollout. Existing
  announcement rooms are not given the new admin posting grant automatically.
- Custom delegated room managers that previously relied on `role.manage` need an effective
  `room.manage` grant for the resources they should administer. Non-owner role assignment is
  also limited to permissions the acting manager possesses.
- The deprecated CLI `--passphrase` argument has been removed from backup, restore, key
  export, and key import commands. Update unattended scripts to use `--passphrase-file` or
  `--passphrase-stdin`.
- Dashboards that query `chatto_service_info{service="*_service"}` must migrate to
  `chatto_model_info{model="*_model"}`.
- Search is disabled by default. To enable the bundled provider, turn on both `[search]` and
  `[search_provider]`, give the provider its own protected local directory, and exclude that
  plaintext-derived index from backups.

## Smaller fixes you'll appreciate

### Privacy and presence

- Presence no longer changes to Away because of input inactivity or tab visibility. You
  control Online, Away, Do Not Disturb, and Look offline directly.

### Messages, media, and calls

- Empty direct-message conversations stay hidden from the other participant until the first
  message is sent.
- Pasted Markdown links preserve their label and destination through repeated edits.
- Enabling rich composer mode on an empty draft no longer inserts a blank line.
- Attachment URLs remain stable during normal viewing, preserving audio, converted GIF, and
  video playback while reactions or other message state changes.
- The Files panel loads attachments only when you open it, reducing unnecessary work while
  switching rooms.
- Fullscreen video's close button stays clear of the iOS status-bar safe area.
- Active calls in direct messages once again show their indicator and participant avatars in
  the sidebar.

### Navigation and notifications

- The desktop room sidebar starts closed in a fresh session and remembers your choice for
  each room while you navigate.
- Installed-app badges show exact direct-message counts without presenting unrelated channel
  notifications as phantom DMs.
- App badges stay synchronized with the authoritative unread notification occurrences across
  regular pushes, visible windows, and supported Safari versions.

### Administration and interface polish

- Permission changes show an immediate pending state instead of appearing unresponsive.
- Server descriptions enforce their byte limit before submission and preserve the current
  draft when a save fails.
- Admin tables and permission matrices have clearer contrast, grouping, and action states.
- Secondary actions, selected rows, and prominent dark-theme buttons are easier to see.
- Remote server discovery continues to work with 0.4 servers that require POST requests.

## GitHub release

The generated GitHub release lists every PR and commit:
[github.com/chattocorp/chatto/releases/tag/v0.5.0](https://github.com/chattocorp/chatto/releases/tag/v0.5.0)
