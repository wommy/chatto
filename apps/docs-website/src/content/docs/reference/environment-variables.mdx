---
title: Environment Variables
description: All environment variables for configuring Chatto.
---

import EnvVar from '../../../components/EnvVar.astro';

Every `chatto.toml` setting can be overridden with an environment variable. The naming pattern is `CHATTO_{SECTION}_{KEY}` in uppercase.

## General

<EnvVar name="CHATTO_LOG_LEVEL" toml="general.log_level" default="info">
  Log level. Values: `debug`, `info`, `warn`, `error`.
</EnvVar>

<EnvVar name="CHATTO_LOG_FORMAT" toml="general.log_format" default="auto">
  Log output format. Values: `auto`, `text`, `json`, `logfmt`. `auto` uses text when Chatto is attached to a terminal and JSON otherwise. Use `json` for Loki/Grafana ingestion so each log line includes a structured `level` field.
</EnvVar>

## Web Server

<EnvVar name="CHATTO_WEBSERVER_URL" toml="webserver.url" required>
  Public URL of the server, used for absolute links, public API asset URLs, and OAuth redirects. Set this to the external HTTPS origin when Chatto runs behind a reverse proxy; forwarded proxy headers are not trusted for public URL generation.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_PORT" toml="webserver.port" default="4000">
  Port to listen on.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_COOKIE_SIGNING_SECRET" toml="webserver.cookie_signing_secret" required>
  256-bit hex secret for signing browser-flow cookies and CSRF proofs. The opaque `chatto_auth_*` authentication cookies do not contain signed user data. Generate with `openssl rand -hex 32`.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_COOKIE_ENCRYPTION_SECRET" toml="webserver.cookie_encryption_secret">
  Optional hex secret for encrypting browser-flow cookies after signing. Must decode to 16, 24, or 32 bytes. Generate an AES-256 key with `openssl rand -hex 32`.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_API_COMPRESSION" toml="webserver.api_compression" default="true">
  Compress eligible ConnectRPC API responses with gzip. Disable this to reduce compressor memory and CPU at the cost of higher network usage. Chatto continues to accept compressed API requests.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_API_COMPRESSION_MIN_BYTES" toml="webserver.api_compression_min_bytes" default="1024">
  Minimum uncompressed ConnectRPC response size, in bytes, eligible for gzip compression. Increase this threshold when small API responses do not save enough bandwidth to justify compressor memory and CPU.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_WEBSOCKET_COMPRESSION" toml="webserver.websocket_compression" default="true">
  Negotiate WebSocket compression. The realtime server compresses frames of at least 1 KiB with a Huffman-only encoder; smaller invalidation and heartbeat frames stay uncompressed and do not instantiate server-side compressor state.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_REALTIME_STEADY_STATE_CONNECTION_CAP" toml="webserver.realtime_steady_state_connection_cap" default="30">
  Maximum concurrent open realtime WebSocket connections per user after catch-up completes. Set to 0 to disable the limit. The limit applies per process or replica, not cluster-wide.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_REQUEST_LOGGING" toml="webserver.request_logging" default="false">
  Log HTTP requests. Successful requests are debug-level and require `CHATTO_LOG_LEVEL=debug`; 4xx and 5xx responses remain visible at warning and error levels.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_TRUSTED_PROXIES" toml="webserver.trusted_proxies">
  Comma-separated IP addresses or CIDR ranges for reverse proxies that Chatto may trust to supply `X-Forwarded-For`, `X-Real-IP`, and `X-Forwarded-Host`. The default is empty, so direct clients cannot spoof audit attribution or WebSocket same-origin checks with forwarded headers. Configure the narrow network used by your proxy—for example, `127.0.0.1` for a same-host proxy or the specific container-network CIDR—and make the proxy overwrite forwarded headers rather than preserving client-supplied values. This does not make forwarded protocol headers authoritative for public URL generation; continue setting `webserver.url`.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_ALLOWED_ORIGINS" toml="webserver.allowed_origins">
  Comma-separated exact additional HTTPS origins for reverse-proxy aliases. Each origin can use browser cookie authentication, publish a matching bundled frontend OAuth identity, and serve an origin-bound MCP resource. Do not include a path, query, or fragment. Do not configure HTTP and HTTPS origins that produce the same request host. Do not use `*`; it permits none of these behaviors.
</EnvVar>

### TLS

Built-in automatic TLS via Let's Encrypt. When enabled, Chatto handles certificate provisioning and renewal automatically.

<EnvVar name="CHATTO_WEBSERVER_TLS_ENABLED" toml="webserver.tls.enabled" default="false">
  Enable automatic TLS via Let's Encrypt. Requires ports 80 and 443 (or custom ports configured below).
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_TLS_DOMAIN" toml="webserver.tls.domain">
  Domain name for the TLS certificate. Required when TLS is enabled.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_TLS_EMAIL" toml="webserver.tls.email">
  Email address for Let's Encrypt notifications. Required when TLS is enabled.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_TLS_CACHE_DIR" toml="webserver.tls.cache_dir" default=".chatto/certs">
  Directory to cache TLS certificates.
</EnvVar>

<EnvVar name="CHATTO_WEBSERVER_TLS_HTTP_PORT" toml="webserver.tls.http_port" default="80">
  Port for the HTTP server (ACME challenges and HTTPS redirect). Use a higher port if running without elevated privileges.
</EnvVar>

## Metrics

<EnvVar name="CHATTO_METRICS_ENABLED" toml="metrics.enabled" default="false">
  Expose a Prometheus-compatible per-process metrics endpoint on a separate internal HTTP listener.
</EnvVar>

<EnvVar name="CHATTO_METRICS_BIND_ADDRESS" toml="metrics.bind_address" default="127.0.0.1">
  Address to bind the metrics listener. Keep this on localhost unless a trusted internal network or sidecar needs direct scrape access.
</EnvVar>

<EnvVar name="CHATTO_METRICS_PORT" toml="metrics.port" default="9090">
  Port for the metrics listener.
</EnvVar>

<EnvVar name="CHATTO_METRICS_PATH" toml="metrics.path" default="/metrics">
  HTTP path for Prometheus scrapes.
</EnvVar>

<EnvVar name="CHATTO_METRICS_PPROF" toml="metrics.pprof" default="false">
  Expose Go pprof debug endpoints on the metrics listener under `/debug/pprof/`. Keep the metrics listener private and enable this only while profiling.
</EnvVar>

## Exporter

The exporter exposes deployment-wide Chatto metrics. Run it as `chatto exporter` against external NATS or an exposed embedded NATS TCP listener, or set `exporter.enabled = true` to embed it inside `chatto run` for single-process installs. Clustered deployments should enable the embedded exporter on at most one replica.

<EnvVar name="CHATTO_EXPORTER_ENABLED" toml="exporter.enabled" default="false">
  Start the deployment-wide Prometheus exporter inside `chatto run`.
</EnvVar>

<EnvVar name="CHATTO_EXPORTER_BIND_ADDRESS" toml="exporter.bind_address" default="127.0.0.1">
  Address to bind the exporter listener. Keep this on localhost unless a trusted internal network or sidecar needs direct scrape access.
</EnvVar>

<EnvVar name="CHATTO_EXPORTER_PORT" toml="exporter.port" default="9100">
  Port for the exporter listener.
</EnvVar>

<EnvVar name="CHATTO_EXPORTER_PATH" toml="exporter.path" default="/metrics">
  HTTP path for Prometheus scrapes.
</EnvVar>

<EnvVar name="CHATTO_EXPORTER_S3_REFRESH_INTERVAL" toml="exporter.s3_refresh_interval" default="15m">
  How often to refresh cached S3 bucket size metrics when S3 asset storage is configured.
</EnvVar>

<EnvVar name="CHATTO_EXPORTER_S3_TIMEOUT" toml="exporter.s3_timeout" default="30s">
  Timeout for one S3 bucket-size refresh.
</EnvVar>

## Model Context Protocol

<EnvVar name="CHATTO_MCP_ENABLED" toml="mcp.enabled" default="false">
  Expose the experimental MCP routes on the public Chatto HTTP server. The endpoints use the `webserver.url` origin and each exact non-wildcard `webserver.allowed_origins` entry with the `/mcp` path.
</EnvVar>

## Shields

<EnvVar name="CHATTO_WEBSERVER_SHIELDS_ENABLED" toml="webserver.shields.enabled" default="false">
  Expose public Shields.io-compatible badge endpoints for aggregate community counts. When enabled, `/.well-known/chatto/shields/online.json` and `/.well-known/chatto/shields/registered.json` return endpoint-badge JSON, while the matching `.png` URLs redirect to Shields.io-rendered badges. Leave disabled for private communities that should not publish size or activity counts.
</EnvVar>

## Message Search

The consumer-facing feature and bundled provider have separate switches. This lets you expose Search through Chatto while running a compatible provider separately, or embed the bundled Bleve provider in `chatto run` for a single-process deployment.

See [Enabling Search](/guides/operations/search/) for a complete setup guide, analyzer names, storage guidance, and troubleshooting.

<EnvVar name="CHATTO_SEARCH_ENABLED" toml="search.enabled" default="false">
  Enable consumer-facing message queries. Compatible clients discover the Search protocol independently and use the Search status RPC to distinguish disabled, indexing, ready, degraded, and unavailable states.
</EnvVar>

<EnvVar name="CHATTO_SEARCH_PROVIDER_ENABLED" toml="search_provider.enabled" default="false">
  Start the bundled Bleve provider inside `chatto run`. This does not by itself expose the consumer-facing Search feature.
</EnvVar>

<EnvVar name="CHATTO_SEARCH_PROVIDER_DIRECTORY" toml="search_provider.directory" default="./data/search">
  Directory for the bundled provider's disposable local index. Protect this directory like message data: it contains plaintext-derived search content, is not included in Chatto backups, and can be rebuilt from retained `EVT` history. Do not set it to the embedded NATS data directory or one of its parent directories.
</EnvVar>

<EnvVar name="CHATTO_SEARCH_PROVIDER_LANGUAGES" toml="search_provider.languages" default="all bundled analyzers">
  Comma-separated Bleve analyzer codes to use when indexing messages and analyzing queries. Supported values are `ar`, `cjk`, `ckb`, `da`, `de`, `en`, `es`, `fa`, `fi`, `fr`, `hi`, `hr`, `hu`, `it`, `nl`, `no`, `pl`, `pt`, `ro`, `ru`, `sv`, and `tr`. Changing the set rebuilds the disposable index. Use `none` for language-neutral literal and typo-tolerant matching only; the TOML equivalent is `languages = []`.
</EnvVar>

## Diagnostics

<EnvVar name="CHATTO_DIAGNOSTICS_STARTUP_CPU_PROFILE" toml="diagnostics.startup_cpu_profile">
  Write a Go CPU profile covering process startup through core boot to this path. Useful for local projection replay benchmarking; disabled when empty.
</EnvVar>

## Operator API

<EnvVar name="CHATTO_OPERATOR_API_ENABLED" toml="operator_api.enabled" default="false">
  Enable the local root-equivalent Operator API Unix socket for `chatto operator ...` commands. The public web listener never serves this API.
</EnvVar>

<EnvVar name="CHATTO_OPERATOR_API_SOCKET_PATH" toml="operator_api.socket_path" default="/tmp/chatto/operator.sock">
  Unix socket path for local operator commands. The socket mode is fixed at `0600`; Docker users should normally run `chatto operator ...` inside the running container with `docker exec`.
</EnvVar>

## Core

<EnvVar name="CHATTO_CORE_EVT_READ_CACHE_IDLE_TTL" toml="core.evt_read_cache_idle_ttl" default="15m">
  Keep an `EVT` record in each server process's timeline read cache for this duration after its last access. A shorter duration reduces idle memory use and can cause more NATS reads. A longer duration can make repeated timeline reads faster and use more memory. Supports values such as `15m` and `1h`.
</EnvVar>

<EnvVar name="CHATTO_CORE_EVT_READ_CACHE_MAX_BYTES" toml="core.evt_read_cache_max_bytes" default="256MiB">
  Set the approximate maximum size of each server process's timeline read cache. Chatto evicts the least-recently-used `EVT` records when the cache exceeds this limit. Supports values such as `128MiB` and `1GiB`. Set this value to `-1` to disable the byte limit. The idle timeout remains active. Chatto logs both effective cache settings at info level during startup. Set `CHATTO_LOG_LEVEL=debug` to also log cache hit and miss summaries, read durations, LRU eviction counts, expiry counts, and cache clears.
</EnvVar>

<EnvVar name="CHATTO_CORE_PROJECTION_SNAPSHOTS" toml="core.projection_snapshots" default="false">
  Persist encrypted projection snapshots and replay only the later `EVT` delta for eligible projections at startup. Chatto treats `EVT` as authoritative and automatically replays it when a snapshot is missing, incompatible, corrupt, expired, or unavailable. User profile state is eligible, but password verifiers, external identities, and OAuth consent remain outside snapshots and cold-replay from focused event subjects. One elected replica attempts publication after boot and every 23–24 hours.
</EnvVar>

<EnvVar name="CHATTO_CORE_PROJECTION_SNAPSHOT_RETENTION" toml="core.projection_snapshot_retention" default="7d">
  Retain projection snapshot generations for this duration. NATS applies the duration as the dedicated Object Store TTL. Chatto uses it as the cutoff for optional S3 expiry. Supports values such as `7d`, `1w`, and `168h`.
</EnvVar>

<EnvVar name="CHATTO_CORE_PROJECTION_SNAPSHOT_S3_CLEANUP" toml="core.projection_snapshot_s3_cleanup" default="true">
  Delete marked S3 projection snapshot generations older than `core.projection_snapshot_retention`. Disable this when the bucket has an external lifecycle policy for `internal/projection-snapshots/`. When enabled, S3 credentials need permission to list that prefix, inspect object metadata, and delete matching objects. Chatto validates the exact internal generation path, content type, and object-purpose marker before deletion; asset paths are never eligible.
</EnvVar>

<EnvVar name="CHATTO_CORE_SECRET_KEY" toml="core.secret_key" required>
  256-bit hex secret for HMAC-derived bearer-token, cookie-session, OAuth-code, account-flow, and invite-link verifiers. Generate with `openssl rand -hex 32`. Keep this stable across restores if you want sessions, pending credentials, and existing invite links to remain valid. Changing it invalidates the old links without deleting their durable invitation records; authorised administrators can copy newly derived links. It also makes projection snapshots unreadable, which Chatto safely rebuilds from `EVT`.
</EnvVar>

## Assets

<EnvVar name="CHATTO_CORE_ASSETS_SIGNING_SECRET" toml="core.assets.signing_secret" required>
  256-bit hex secret for signing asset URLs. Generate with `openssl rand -hex 32`.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_MAX_UPLOAD_SIZE" toml="core.assets.max_upload_size" default="25 MB">
  Max upload size for images and non-video files (e.g., `25MB`, `25 MiB`). Videos use a separate limit — see `CHATTO_VIDEO_MAX_UPLOAD_SIZE`.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_STORAGE_BACKEND" toml="core.assets.storage_backend" default="nats">
  Storage backend for persisted assets. Values: `nats`, `s3`.
</EnvVar>

### S3 Storage

Only used when `storage_backend` is set to `s3`. See the [S3 Storage guide](/guides/infrastructure/s3-storage/) for setup instructions.

<EnvVar name="CHATTO_CORE_ASSETS_S3_ENDPOINT" toml="core.assets.s3.endpoint">
  S3 endpoint URL (e.g., `s3.amazonaws.com` for AWS, or a custom endpoint for S3-compatible services).
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_BUCKET" toml="core.assets.s3.bucket">
  S3 bucket name. Created automatically on startup if it doesn't exist.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_PATH_PREFIX" toml="core.assets.s3.path_prefix">
  Optional object key prefix for all S3 assets. Leave empty to store objects at the bucket root.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_REGION" toml="core.assets.s3.region">
  AWS region. Optional for non-AWS S3-compatible services.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_ACCESS_KEY_ID" toml="core.assets.s3.access_key_id">
  S3 access key ID.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_SECRET_ACCESS_KEY" toml="core.assets.s3.secret_access_key">
  S3 secret access key.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_USE_SSL" toml="core.assets.s3.use_ssl" default="true">
  Use HTTPS for S3 connections.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_S3_PATH_STYLE" toml="core.assets.s3.path_style" default="auto">
  Override S3 URL addressing. By default, Chatto uses virtual-hosted addressing for AWS S3 endpoints and path-style addressing for custom S3-compatible endpoints.
</EnvVar>

### Image Cache

<EnvVar name="CHATTO_CORE_ASSETS_CACHE_ENABLED" toml="core.assets.cache.enabled" default="false">
  Enable caching for resized image transforms. Reduces CPU usage for repeated thumbnail generation.
</EnvVar>

<EnvVar name="CHATTO_CORE_ASSETS_CACHE_TTL" toml="core.assets.cache.ttl" default="7d">
  Time-to-live for cached images. Supports durations like `7d`, `1w`, `168h`.
</EnvVar>

## NATS

<EnvVar name="CHATTO_NATS_REPLICAS" toml="nats.replicas" default="1">
  Number of replicas for JetStream streams, KV buckets, and object stores. Must be an odd number (`1`, `3`, or `5`) for quorum. Set to `3` or `5` when running a NATS cluster for fault tolerance.
</EnvVar>

### Embedded Server

<EnvVar name="CHATTO_NATS_EMBEDDED_ENABLED" toml="nats.embedded.enabled" default="true">
  Enable the embedded NATS server. Set to `false` when connecting to an external NATS server.
</EnvVar>

<EnvVar name="CHATTO_NATS_EMBEDDED_PORT" toml="nats.embedded.port" default="0">
  Embedded NATS server port. Leave unset or set to `0` for in-process only mode. Set to `4222` or another port to expose a TCP listener for NATS CLI/admin commands.
</EnvVar>

<EnvVar name="CHATTO_NATS_EMBEDDED_BIND_ADDRESS" toml="nats.embedded.bind_address" default="127.0.0.1">
  Embedded NATS server bind address.
</EnvVar>

<EnvVar name="CHATTO_NATS_EMBEDDED_HTTP_PORT" toml="nats.embedded.http_port" default="0">
  NATS monitoring/stats HTTP port. Set to `0` to disable. Useful for debugging with the NATS dashboard.
</EnvVar>

<EnvVar name="CHATTO_NATS_EMBEDDED_DATA_DIR" toml="nats.embedded.data_dir" default="./data">
  JetStream data directory for the embedded server.
</EnvVar>

<EnvVar name="CHATTO_NATS_EMBEDDED_AUTH_TOKEN" toml="nats.embedded.auth_token">
  Auth token for the embedded NATS server. When the embedded TCP port is enabled, Chatto derives matching `nats.client` defaults for CLI/admin commands unless you override them explicitly.
</EnvVar>

### Client

<EnvVar name="CHATTO_NATS_CLIENT_URL" toml="nats.client.url">
  NATS server URL to connect to when using external NATS or an exposed embedded TCP listener. Supports comma-separated URLs for cluster failover (e.g., `nats://n1:4222,nats://n2:4222`).
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_AUTH_METHOD" toml="nats.client.auth_method">
  NATS authentication method for external NATS or an exposed embedded TCP listener. Values: `none`, `token`, `userpass`, `credentials`, `nkey`.
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_TOKEN" toml="nats.client.token">
  NATS auth token (when using `token` auth method). When explicitly set alongside embedded NATS, it must match `CHATTO_NATS_EMBEDDED_AUTH_TOKEN`.
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_USERNAME" toml="nats.client.username">
  NATS username (when using `userpass` auth method).
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_PASSWORD" toml="nats.client.password">
  NATS password (when using `userpass` auth method).
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_CREDENTIALS_FILE" toml="nats.client.credentials_file">
  Path to `.creds` file (when using `credentials` auth method).
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_NKEY_SEED" toml="nats.client.nkey_seed">
  NKey seed (when using `nkey` auth method).
</EnvVar>

<EnvVar name="CHATTO_NATS_CLIENT_CA_CERT" toml="nats.client.ca_cert">
  PEM-encoded CA certificate for verifying the NATS server's TLS certificate. When set, the NATS client connection uses TLS.
</EnvVar>

## Authentication

<EnvVar name="CHATTO_AUTH_ACCOUNT_CREATION_POLICY" toml="auth.account_creation_policy" default="open">
  Controls who may create a new account. Use `open` for public self-service account creation or `invite_only` to require a valid invite link for direct registration and external-provider auto-provisioning. Existing account sign-in, account linking, and operator-created accounts are unaffected. Upgrade every serving replica before enabling `invite_only`.
</EnvVar>

<EnvVar name="CHATTO_AUTH_DIRECT_REGISTRATION" toml="auth.direct_registration" default="true">
  Enable direct (email/password) registration. When `false`, the registration page is hidden and the registration API returns `403`. Existing users can still sign in through enabled password login or configured SSO providers.
</EnvVar>

<EnvVar name="CHATTO_AUTH_DIRECT_LOGIN" toml="auth.direct_login" default="true">
  Enable direct sign-in with a username or email address and password. When `false`, the password form and recovery link are hidden and the password login endpoint returns `403`. Existing sessions, registration, and authenticated password management are unaffected.
</EnvVar>

<EnvVar name="CHATTO_AUTH_TOKEN_TTL" toml="auth.token_ttl" default="90d">
  Renewal window for active human sessions and initial lifetime of each same-origin cookie credential. Active bearer sessions advance this window during background refresh. In the final quarter, the bundled browser automatically renews the same cookie handle through a dedicated route. Ordinary validation does not write the session. An inactive session expires after one complete window. Supports durations like `90d` or `2160h`.
</EnvVar>

<EnvVar name="CHATTO_AUTH_ACCESS_TOKEN_TTL" toml="auth.access_token_ttl" default="15m">
  Fixed lifetime of each human bearer access token. Validation does not renew it; compatible clients rotate the paired refresh credential before expiry. Supports durations like `15m`, `30m`, or `1h`.
</EnvVar>

<EnvVar name="CHATTO_AUTH_EMAIL_OTP_THROTTLING_ENABLED" toml="auth.email_otp.throttling_enabled" default="true">
  Enable throttling for registration and email-verification code delivery and wrong-code attempts. Set to `false` to disable these throttles while keeping OTP TTL enforcement.
</EnvVar>

<EnvVar name="CHATTO_AUTH_EMAIL_OTP_TTL" toml="auth.email_otp.ttl" default="30m">
  How long registration and email-verification codes remain valid. Supports durations like `15m`, `30m`, or `1h`.
</EnvVar>

<EnvVar name="CHATTO_AUTH_EMAIL_OTP_MAX_DELIVERED_CODES" toml="auth.email_otp.max_delivered_codes" default="10">
  Maximum successfully delivered registration or email-verification codes allowed for the same email challenge before fresh code requests are throttled.
</EnvVar>

<EnvVar name="CHATTO_AUTH_EMAIL_OTP_MAX_WRONG_ATTEMPTS" toml="auth.email_otp.max_wrong_attempts" default="5">
  Maximum wrong-code submissions allowed for the same email challenge before the challenge is exhausted until its TTL expires.
</EnvVar>

### External Login Providers

External login providers are configured as repeated `[[auth.providers]]` tables in `chatto.toml`.
Supported provider types are `oidc`, `github`, `gitlab`, `google`, and `discord`.

<EnvVar name="CHATTO_AUTH_PROVIDERS_<index>_<field>" toml="auth.providers">
  Counted environment-variable equivalent of repeated `[[auth.providers]]` tables. Indexes start at `0` and must not have gaps. Supported fields are `ID`, `TYPE`, `LABEL`, `CLIENT_ID`, `CLIENT_SECRET`, `ISSUER_URL`, `SCOPES`, `REQUEST_EMAIL`, `AUTO_PROVISION`, and `PROVIDER_OPTIONS_<KEY>`. `CLIENT_SECRET` is optional only for public OIDC clients; OAuth provider types still require it.
</EnvVar>

For compatibility with older single-provider OIDC deployments, Chatto also accepts `CHATTO_AUTH_OIDC_ENABLED`, `CHATTO_AUTH_OIDC_ISSUER_URL`, `CHATTO_AUTH_OIDC_CLIENT_ID`, `CHATTO_AUTH_OIDC_CLIENT_SECRET`, and optional `CHATTO_AUTH_OIDC_LABEL`. These create one `oidc` provider with `id = "oidc"`. Do not combine them with counted `CHATTO_AUTH_PROVIDERS_<index>_*` variables.

```toml
[[auth.providers]]
id = "chatto-hub"
type = "oidc"
label = "Chatto Hub"
issuer_url = "https://hub.chatto.dev"
client_id = "..."
client_secret = "..."
auto_provision = false
```

Provider IDs are used in login URLs and external identity links, so do not change them after users link accounts through that provider.

`request_email` defaults to `false`, so Chatto does not request provider email scopes unless you opt in. Set `request_email = true` when you want provider-verified email claims. OIDC providers can run with only `openid profile`; Chatto links and signs in accounts by issuer and subject even when no email claim is returned.

For a CIMD-capable OIDC provider, set `CHATTO_AUTH_PROVIDERS_<index>_CLIENT_ID` to `{webserver.url}/oauth/client-metadata.json` and omit `CHATTO_AUTH_PROVIDERS_<index>_CLIENT_SECRET`. Chatto then publishes the public client metadata at that URL. See the [External Login Providers guide](/guides/integrations/external-login-providers/#cimd-public-clients) for an example.

Set `auto_provision = true` only for providers whose users should be allowed to create accounts. Unlinked identities from providers without auto-provisioning can still be linked by existing signed-in users from account settings.

See [External Login Providers](/guides/integrations/external-login-providers/) for callback URLs, provider-specific examples, and discovery behavior.

## Transactional Email

SMTP is the default transactional email transport. Set `email.transport = "jmap"` when your provider exposes JMAP submission and you want Chatto to authenticate with a bearer token instead.

<EnvVar name="CHATTO_EMAIL_TRANSPORT" toml="email.transport" default="smtp">
  Transactional email transport. Supported values are `smtp` and `jmap`.
</EnvVar>

### JMAP

<EnvVar name="CHATTO_EMAIL_JMAP_SESSION_URL" toml="email.jmap.session_url">
  HTTPS JMAP session resource URL. Required when `email.transport` is `jmap`.
</EnvVar>

<EnvVar name="CHATTO_EMAIL_JMAP_ACCESS_TOKEN" toml="email.jmap.access_token">
  Bearer access token for the JMAP account. Required when `email.transport` is `jmap`.
</EnvVar>

<EnvVar name="CHATTO_EMAIL_JMAP_FROM" toml="email.jmap.from">
  From address for outgoing email. It must match a sending identity in the JMAP account. Required when `email.transport` is `jmap`.
</EnvVar>

<EnvVar name="CHATTO_EMAIL_JMAP_ACCOUNT_ID" toml="email.jmap.account_id">
  Optional JMAP account ID. By default, Chatto uses the session's primary submission account.
</EnvVar>

<EnvVar name="CHATTO_EMAIL_JMAP_IDENTITY_ID" toml="email.jmap.identity_id">
  Optional JMAP identity ID. By default, Chatto uses the identity matching `email.jmap.from`.
</EnvVar>

<EnvVar name="CHATTO_EMAIL_JMAP_DRAFT_MAILBOX_ID" toml="email.jmap.draft_mailbox_id">
  Optional JMAP Drafts mailbox ID. By default, Chatto uses the mailbox with the `drafts` role.
</EnvVar>

### SMTP

<EnvVar name="CHATTO_SMTP_ENABLED" toml="smtp.enabled" default="false">
  Enable SMTP email sending.
</EnvVar>

<EnvVar name="CHATTO_SMTP_HOST" toml="smtp.host">
  SMTP server hostname.
</EnvVar>

<EnvVar name="CHATTO_SMTP_PORT" toml="smtp.port">
  SMTP port. Common values: `587` for STARTTLS, or `465` for implicit TLS/SMTPS.
</EnvVar>

<EnvVar name="CHATTO_SMTP_TLS" toml="smtp.tls" default="mandatory">
  SMTP TLS policy. Use `mandatory` to require STARTTLS on port `587`, or `implicit` for SMTPS on port `465`. When this value is omitted or left as `mandatory`, Chatto automatically uses implicit TLS for port `465`. Use `opportunistic` only when the SMTP server cannot support mandatory STARTTLS and plaintext fallback is explicitly acceptable.
</EnvVar>

<EnvVar name="CHATTO_SMTP_TLS_SERVER_NAME" toml="smtp.tls_server_name">
  SMTP TLS server name for certificate verification and SNI. Use when `smtp.host` is an IP address or internal alias but the certificate is issued for a DNS name.
</EnvVar>

<EnvVar name="CHATTO_SMTP_TLS_SKIP_VERIFY" toml="smtp.tls_skip_verify" default="false">
  Disable SMTP TLS certificate verification. Insecure; use only for trusted internal SMTP servers with self-signed or mismatched certificates.
</EnvVar>

<EnvVar name="CHATTO_SMTP_USERNAME" toml="smtp.username">
  SMTP username.
</EnvVar>

<EnvVar name="CHATTO_SMTP_PASSWORD" toml="smtp.password">
  SMTP password.
</EnvVar>

<EnvVar name="CHATTO_SMTP_FROM" toml="smtp.from">
  From address for outgoing emails.
</EnvVar>

## Push Notifications

<EnvVar name="CHATTO_PUSH_ENABLED" toml="push.enabled" default="false">
  Enable Web Push notifications.
</EnvVar>

<EnvVar name="CHATTO_PUSH_VAPID_PUBLIC_KEY" toml="push.vapid_public_key">
  VAPID public key for Web Push. Generate with `npx web-push generate-vapid-keys`.
</EnvVar>

<EnvVar name="CHATTO_PUSH_VAPID_PRIVATE_KEY" toml="push.vapid_private_key">
  VAPID private key for Web Push.
</EnvVar>

<EnvVar name="CHATTO_PUSH_VAPID_SUBJECT" toml="push.vapid_subject">
  VAPID subject identifier. Use an operator email address, optionally prefixed with `mailto:`, or an `https:` contact URL.
</EnvVar>

## Asset Processing

<EnvVar name="CHATTO_ASSET_PROCESSING_ENABLED" toml="asset_processing.enabled" default="false">
  Start the built-in durable asset-processing worker inside `chatto run`. `chatto init` writes this as `true` for a self-contained default installation. The explicit `chatto asset-processing` command runs regardless of this setting.
</EnvVar>

<EnvVar name="CHATTO_ASSET_PROCESSING_FFMPEG_PATH" toml="asset_processing.ffmpeg_path">
  Path to the `ffmpeg` binary. If not set, Chatto looks for `ffmpeg` in `PATH`.
</EnvVar>

<EnvVar name="CHATTO_ASSET_PROCESSING_FFPROBE_PATH" toml="asset_processing.ffprobe_path">
  Path to the `ffprobe` binary. If not set, Chatto looks for `ffprobe` in `PATH`.
</EnvVar>

<EnvVar name="CHATTO_ASSET_PROCESSING_MAX_CONCURRENT_JOBS" toml="asset_processing.max_concurrent_jobs" default="2">
  Maximum number of jobs to process concurrently in each worker process. Higher values use more CPU and memory.
</EnvVar>

<EnvVar name="CHATTO_ASSET_PROCESSING_TEMP_DIR" toml="asset_processing.temp_dir">
  Directory for temporary files during transcoding. Defaults to the system temp directory.
</EnvVar>

## Video Processing

<EnvVar name="CHATTO_VIDEO_ENABLED" toml="video.enabled" default="false">
  Allow video uploads and enqueue their derivative processing. At least one embedded or standalone asset-processing worker must have `ffmpeg` and `ffprobe` available.
</EnvVar>

<EnvVar name="CHATTO_VIDEO_MAX_UPLOAD_SIZE" toml="video.max_upload_size" default="100 MB">
  Max upload size for video files (e.g., `100MB`, `200 MiB`). Only applies when video uploads are enabled. Non-video files use `CHATTO_CORE_ASSETS_MAX_UPLOAD_SIZE` instead.
</EnvVar>

## LiveKit (Voice & Video Calls)

<EnvVar name="CHATTO_LIVEKIT_ENABLED" toml="livekit.enabled" default="false">
  Enable LiveKit voice and video call support. Requires a running LiveKit server.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_URL" toml="livekit.url">
  LiveKit server WebSocket URL. Use `ws://` for development, `wss://` for production.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_API_KEY" toml="livekit.api_key">
  LiveKit API key for authentication.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_API_SECRET" toml="livekit.api_secret">
  LiveKit API secret for signing JWT tokens. Keep this confidential.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_WEBHOOK_URL" toml="livekit.webhook_url">
  URL where LiveKit sends webhook events. Defaults to `{webserver.url}/webhooks/livekit`.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_SERVER_ID" toml="livekit.server_id">
  Unique identifier for this Chatto server, prefixed to deterministic LiveKit room names. This value must match across replicas of one Chatto server, and should differ only across different Chatto servers sharing the same LiveKit cluster. When empty, room names use the unprefixed format. The old `CHATTO_LIVEKIT_INSTANCE_ID` / `livekit.instance_id` name is still accepted as a deprecated alias.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_INSTANCE_ID" toml="livekit.instance_id">
  Deprecated alias for `CHATTO_LIVEKIT_SERVER_ID` / `livekit.server_id`. Prefer the server ID name in new deployments.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_WEBHOOK_API_KEY" toml="livekit.webhook_api_key">
  API key that LiveKit uses to sign webhook requests. When multiple Chatto servers share a LiveKit cluster, the webhook signing key may differ from the per-server API key. Falls back to `CHATTO_LIVEKIT_API_KEY` when not set.
</EnvVar>

<EnvVar name="CHATTO_LIVEKIT_WEBHOOK_API_SECRET" toml="livekit.webhook_api_secret">
  API secret for validating webhook signatures. Falls back to `CHATTO_LIVEKIT_API_SECRET` when not set.
</EnvVar>

## Owners

<EnvVar name="CHATTO_OWNERS_EMAILS" toml="owners.emails">
  Comma-separated list of email addresses that confer server-owner status. A user with any verified email matching the list gets full server access (including `/admin` routes). Only verified email addresses are matched.
</EnvVar>

## Limits

Server-wide resource limits. Use `-1` for unlimited (the default), `0` to disable creation entirely, or any positive integer to cap.

<EnvVar name="CHATTO_LIMITS_MAX_USERS" toml="limits.max_users" default="-1">
  Maximum number of verified accounts on this server. An account counts once it has at least one verified sign-in factor: a verified email or a linked SSO identity. Enforced when accounts are created and when the first verified factor is added. Note that the check is non-atomic, so a burst at the boundary can briefly overshoot by one or two.
</EnvVar>
